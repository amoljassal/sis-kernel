[toolchain]
# Pin to a known-good nightly. If CI images don't have this exact build,
# bump to the latest nightly that still compiles your tree.
channel = "nightly-2025-08-01"
components = ["rust-src", "llvm-tools-preview", "clippy", "rustfmt"]
targets = ["x86_64-unknown-none"]